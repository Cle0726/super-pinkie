# 更新记录

## 2.5.5

- 加强 CLE Kk 执行控制层：实际工具结果、文件变化与独立验收共同决定任务能否交付；未执行、验收失败或只有口头承诺时，不能按完成状态结束。
- 外部 Skill 验收绑定本轮任务的证据与验证程序，防止旧产物、替换验收程序或自写成功记录被误当成完成证明。
- 档位续跑与子代理保留用户选择的模型，统一汇总执行状态与最终交付；中断恢复继续沿用已有任务和会话。
- Windows 保留主题配色，同时使用更适合 Windows 的拖动区域和最小化、最大化、关闭按钮。
- 发布前自动运行全部 Node.js 与 Python 回归测试；任一测试失败都阻止 macOS、Windows 和源码包发布，发布标签必须与内置版本一致。
- 升级继续保留已有会话、项目、人格、上下文、四档设定、开屏视频和 UI 素材。

## 2.5.4

- 修复 Windows 无边框 App 难以移动和无法可靠调整尺寸的问题：加入清晰但克制的拖动把手、双击最大化/还原、四边四角原生缩放，并显式启用可调整窗口与系统阴影边界。
- Windows 最小窗口调整为 760×500；更新弹窗会根据窗口高度收紧并在必要时滚动，避免按钮被裁掉。
- 修复极致思考档位关闭后后台运行仍锁住输入框的问题；取消时同时终止父轮次与派生任务，遗留档位会自动清理。
- 档位最终交付改为直接给成品或答案，不再把代理数量、流水线和审议过程当成正文；迟到的内部续跑不会重复回复。
- 想法模式加入明确取舍、主动反驳和平衡证据的主见型协作人格；既有会话、项目、人格自定义、视频和 UI 素材均不覆盖。

## 2.5.0

- Windows EXE 升级为真正的自包含桌面 App：内置固定版本的 Node.js、OpenClaw、Python 依赖、网关、派对空间、灵感圆桌、语音和重试代理，双击后直接进入内嵌聊天，不再要求用户安装命令行环境。
- Windows 加入内嵌 Edge WebView2 窗口、原生项目文件夹选择、SAPI 语音听写、无边框窗口控制和一次性视频开屏；加载较慢时定格尾帧等待。
- Windows 网关每 2 秒健康检查，连续失败后自动拉起；模型与工具子进程使用跨平台增量管道读取，避免 Windows 下回复积压到末尾才出现。
- 保留原有维护控制台作为 `--control-center` 后备入口；用户 Key、聊天记录、项目和工作区保持在 EXE 外，桌面 App 启动时只补缺失默认文件，不覆盖已有的人格或提示词。
- macOS 自包含包同步带入跨平台进程流模块，保持派对空间与灵感圆桌兼容，不修改既有人格、四档模式、上下文或 UI 素材。

## 2.4.1

- 修复本地模型中继看门狗将正常鉴权响应误判为掉线的问题；改为每 2 秒探测、连续两次真实断联才重启，并加入重启冷却，避免看门狗本身反复打断回复。
- 修复流式光标触发自身 DOM 监听死循环，以及 Markdown 分块重绘导致旧段落反复淡入的问题；回复与工具过程改为连续增量展示。
- 全模式加入原生手动上下文整理按钮，只压缩较早内容并保留最近对话与工作检查点，不向聊天记录发送命令。
- 极致思考进度改为平滑流动条和增量更新，主会话汇总规划、求解、批评与仲裁进度，派生会话不再污染左侧记录。
- 开屏视频仅播放一次；若主界面尚未就绪，则停在视频尾帧继续等待，并保留「来啦～老弟」入场接力。

## 2.2.0

- macOS App 改为自包含发行包，内置 Node.js、OpenClaw 网关、Python、语音、派对空间与灵感圆桌服务；双击即可启动，用户配置和聊天记录继续保存在 App 外部。
- 启动画面改为代码绘制的轻量动效，移除居中的贴图感图片，并固定 WKWebView 透明底色，解决启动和切换时透明度闪烁。
- 全面检查四模式、概览、设置、派对空间和灵感圆桌，修复嵌套路由皮肤丢失、模式头像裂图、侧栏会话覆盖底栏和横向溢出。
- 四模式素材、用量统计和切换动画改用站点根路径，设置页与概览页不再错误请求 `/settings/laolao-*`。
- 灵感圆桌增加安全的 Markdown 展示，标题、清单、表格和代码正常排版，不再显示成一大段原始符号。
- Windows 注入器兼容当前 `dist/control-ui` 与旧版 `ui` 目录，GitHub Release 同时提供 Windows EXE、自包含 macOS App 和完整源码包。
- 完整 UI 与运行逻辑测试扩展到 74 项，并在发布前全部通过。

## 2.1.0

- 在唠嗑模式与想法模式之间新增「项目模式」。
- 新增独立项目代理、工作区和项目人格，不修改原有三套人格文件。
- 新增档案策划型高清头像、矢量长按钮、聊天壁纸与模式切换画面。
- 模式切换、启动接力、头像、面包屑和缓存更新为四模式联动。
- 四个模式的聊天记录、置顶、项目分组和文件夹绑定完全分区；旧共享数据按代理安全迁移并保留恢复副本。
- 项目模式改为认真可靠的工作人格，专称用户为“老板”，并要求每个会话重新完整加载所需 Skill。
- 唠嗑模式加入糖果彩带进度动效，想法模式加入灵感气泡流动进度动效。

## 2.0.1

- macOS 桌面成品统一改名为「超級碧琪.app」。
- Windows 桌面成品统一改名为「超級碧琪.exe」。
- 安装、更新与卸载流程同步新名称，并在更新时备份后移除旧名称 App。

## 2.0.0

- 加入完整的「来啦～老弟」透明玻璃 UI 与全部资源。
- 加入唠嗑、想法、无限制三模式及原样人格文件。
- 加入模式无缝切换、稳定进度、转场动画与对应头像/壁纸。
- 加入原生听写、按语音回合朗读、长回复摘要朗读和晓伊 TTS 服务。
- 加入图片点击放大、复制粘贴、窗口拖动与圆角桌面壳。
- 加入侧边栏项目文件夹选择、Finder 定位、路径复制、重命名和项目内新会话。
- 加入完整 macOS 构建、一键安装、卸载、Git 拉取更新和 UI 自动重应用。
- 加入 GitHub Actions 校验、App 构建与 Release 发布。
